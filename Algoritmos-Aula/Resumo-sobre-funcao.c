#include <stdio.h>


double funcao( "Dados que seu codigo precisa, como: Float A / Float B / Float C" ){
//^-- Tipo que a função retornara
    dado = A + B + C;
    return dado;
//           ^--- O que sera retornado para a função main()
}
int main()
{

    double Resultado;
   
    Resultado = funcao( "Dados que seu codigo vai oferecer, como: A / B / C" )
//     ^--Aonde serão armazenados
    return 0;
}